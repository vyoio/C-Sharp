//Ternary Operator - evaluates two values and returns one based on the Boolean expression

            String city = "Cleveland";

            String country = (city == "Cleveland" ? "United States" : "Unknown");

            Console.WriteLine(country);

            Console.ReadLine();
